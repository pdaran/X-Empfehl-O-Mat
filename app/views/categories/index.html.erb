<!DOCTYPE html>
<html lang="de">
<div>
<main>
  <h1 class="text-center mb-10">Kategorien</h1>
  <div class="flex flex-wrap min-h-screen justify-center items-center gap-4">
    <% @categories.each_slice(3) do |category_row| %>
      <div class="flex justify-center h-full w-full">
        <% category_row.each do |category| %>
          <div class="rounded-md items-center p-2 w-1/3 text-center m-4 flex flex-col justify-center transition-transform duration-300 ease-in-out hover:scale-110">
            <a href="<%= category_path(category) %>" class="text-gray-800 hover:underline">
              <%= category.title %>
            </a>
             <div class="w-full flex justify-center">
              <% if category.image.attached? %>
               <a href=" <%= category_path(category) %>"> 
                  <%= image_tag category.image , class:"object-cover mx-auto my-auto rounded-md w-56 h-56" %>
               </a>
             <% else %>
               <!-- Placeholder image or alternate content if no image is attached -->
                <%= image_tag 'placeholder.jpeg', class:"object-cover mx-auto my-auto" %>
             <% end %>
      <!-- Additional product details -->
    </div>
          </div>
        <% end %>
      </div>
    <% end %>
    <!-- "Kategorie" box -->
    <div class="bg-gray-300 order-4 rounded-md text-center block text-3xl p-4 mb-4 transition ease-in-out delay-150 hover:-translate-y-1 hover:scale-110 duration-300 hover:opacity-80 w-1/3">
      <%= link_to new_category_path do %>
        <i class="fas fa-plus"></i>
      <% end %>
    </div>
  </div>
</main>

</div>
</html>