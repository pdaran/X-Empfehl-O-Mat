<!DOCTYPE html>
<html lang="de">
<div class="bg-gray-100 h-screen flex items-center justify-center">
  <main>
    <h1 class="text-center mb-6 text-4xl font-bold">Categories</h1>
    <p class="text-center mb-10 text-lg">Fügen Sie neue Kategorien hinzu.</p>
    <div class="bg-white shadow-lg rounded-md p-12 w-5/6 mx-auto">
      <div class="flex flex-wrap justify-center items-center gap-4">
        <% @categories.each_slice(3) do |category_row| %>
          <div class="flex flex-col w-54 h-54 md:flex-row justify-center md:h-full md:w-full">
            <% category_row.each do |category| %>
              <div class="rounded-md items-center p-2 w-md md:w-1/3 text-center m-4 flex flex-col justify-center transition-transform duration-300 ease-in-out hover:scale-105">
                <a href="<%= category_path(category) %>" class="text-gray-800 hover:underline">
                  <%= category.title %>
                  <% if category.status == 'active' %>
                    <span class="blinking active"></span>
                  <% elsif category.status == 'deactivated' %>
                    <span class="blinking deactivated"></span>
                  <% end %>
                </a>
                <% if category.image.attached? && category.status == 'active' %>
                  <%= link_to image_tag(category.image.variant(:large), class: "mx-auto my-auto rounded-md"), category_path(category) %>
                <% elsif category.status == 'deactivated' %>
                  <%= link_to image_tag(category.image.variant(:large), class: "mx-auto my-auto rounded-md deactivated-image"), category_path(category) %>
                <% else %>
                  <%= image_tag 'placeholder.jpeg', class: "object-cover mx-auto my-auto rounded-md w-64 h-64 md:w-64 md:h-64" %>
                <% end %>
                <!-- Additional product details -->
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="bg-gray-300 rounded-md text-center text-3xl p-4 mt-8 mb-4 transition ease-in-out delay-150 hover:-translate-y-1 hover:scale-110 duration-300 hover:opacity-80 flex justify-center items-center w-1/3 h-auto mx-auto">
        <%= link_to new_category_path do %>
          <i class="fas fa-plus"></i>
        <% end %>
      </div>
    </div>
  </main>
</div>
</html>
