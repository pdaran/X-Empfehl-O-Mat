<div class="flex items-center justify-center h-screen">
  <div class="border-2 border-black p-16 rounded">
    <h1 class="text-4xl font-bold mb-6 text-center uppercase font-montserrat">
      <%= t('category.new') %>
    </h1>

    <%= render "form" %>

    <div class="text-center items-center justify-center m-2 p-2">
      <button class="bg-gray-400 hover:bg-gray-300 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
        <%= link_to t('category.back'), shop_categories_path(@shop) %>
      </button>
    </div>
  </div>
</div>
