<div class="flex flex-col justify-center items-center h-screen mt-14 mb-14">
  <div class="border-2 border-black p-16 rounded">
    <h1 class="text-4xl font-bold mb-6 text-center uppercase font-montserrat">
      <%= t('category.new') %>
    </h1>

    <%= render "form" %>

    <div class="text-center mt-12">
      <%= link_to t('category.back'), shop_categories_path(@shop), class: "text-white bg-black hover:bg-gray-600 font-bold py-2 px-4 uppercase font-montserrat" %>
    </div>
  </div>
</div>
