
    <div class="container">
      <h1><%= t('mailer.password_reset') %></h1>
      <div>
        <p><%= t('hello')  %><%= @shop.name %>,</p>
        <p><%= t('mailer.reset_link') %></p>
          <%= link_to 'Reset Password', edit_password_reset_url(id: @shop.password_reset_token, locale: I18n.locale) %>
        <p><%= t('mailer.link_validity') %><%= t('mailer.no_request_password') %></p>
        <p><%= t('mailer.regards') %></p>
        <p><%= t('mailer.your') %></p>
      </div>
      <div class="footer">
        &copy; <%= t('mailer.footer') %>
      </div>
    </div>
