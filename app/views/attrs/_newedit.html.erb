<%= render 'form', attr: @attr %>

<div class="text-center items-center justify-center mt-12 p-2">
  <%= link_to t('category.back'), edit_shop_category_path(@category.shop, @category), class: "text-white bg-black hover:bg-gray-600 font-bold py-2 px-4 uppercase font-montserrat" %>
</div>