<div class="flex items-center justify-center h-screen my-36 px-4">
  <div class="border-2 border-black p-16">
    <h1 class="text-4xl font-bold mb-6 text-center uppercase font-montserrat sm:text-2xl">
      <%= t('shop.edit') %>
    </h1>

      <%= render "form" %>

    <div class="text-center items-center justify-center mt-12 p-2">
      <%= link_to t('category.back'), root_path, class: "text-white bg-black hover:bg-gray-600 font-bold py-2 px-4 uppercase font-montserrat" %>
    </div>
  </div>
</div>

